using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface EventInterface
{
    // Start is called before the first frame update
    void BindEvent(GameObject src);

}
